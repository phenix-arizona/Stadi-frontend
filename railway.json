{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
```

**Step 3 — Make sure `VITE_API_URL` is set in Railway Variables BEFORE pushing:**
```
VITE_API_URL=https://stadi-backend-production.up.railway.app/api